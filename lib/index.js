
var Application = require('./application');

var app = Application;
app.listen();

module.exports = app;

//var repl = require('repl').start('> ');

//repl.context.app = app;
//repl.context.Models = Models = require('./models');